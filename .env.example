# =============================================================================
# Azure OpenAI (used by LLM agents in both pipelines)
# =============================================================================
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-06-01
AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# =============================================================================
# Typesense (search + write for exploration themes and also-buy)
# =============================================================================
TYPESENSE_NODE_HOST=your-typesense-host
TYPESENSE_PORT=443
TYPESENSE_API_KEY=your-typesense-api-key

# =============================================================================
# Embeddings service
# =============================================================================
EMBEDDINGS_SERVICE_URL=https://your-embeddings-service.internal/embed

# =============================================================================
# Exploration Themes
# =============================================================================
# Optional — defaults to "themes"
EXPLORATION_THEMES_COLLECTION=themes

# Optional — defaults to "data/themes.yaml"
THEMES_FILE=data/themes.yaml

# =============================================================================
# Also-Buy pipeline (separate from exploration themes)
# =============================================================================
MARKETPLACER_URL=https://your-marketplacer-graphql-endpoint
MARKETPLACER_TOKEN=your-marketplacer-token

# =============================================================================
# Azure Blob Storage
# =============================================================================
AZURE_STORAGE_CONNECTION_STRING=your-azure-storage-connection-string
